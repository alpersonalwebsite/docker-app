const app = require('../server')
const http = require('http')
const assert = require('assert')
const {
  HOST,
  PORT
} = require('../constants')

describe('', () => {
  before('some test', () => {

    it('should return a 200', () => {})
  })

})